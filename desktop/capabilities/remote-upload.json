{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-upload",
  "description": "The workspace webview shows the ship-served lattice client (any user-configured URL, so the pattern is any host). pick_upload is the only command it may invoke: it opens a native dialog and returns only what the user picked, so a hostile page gets nothing without the user choosing files in a visible OS dialog.",
  "windows": ["workspace"],
  "remote": {
    "urls": ["http://*", "http://*:*", "https://*", "https://*:*"]
  },
  "permissions": ["allow-pick-upload"]
}
