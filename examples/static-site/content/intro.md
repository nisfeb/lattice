# Introduction

Welcome to a **static site** built entirely inside lattice.

Each page here is an ordinary markdown page — viewable on its own URL. The
`/site` index composes them into a nav using a *directory dependency*.
