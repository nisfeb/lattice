"""lattice_fs: the lattice projection for grubbery-fs.

The single place that names page-save / page-tree / page-source / the err grub
/ kind<->ext / the page-with-children convention. A second grubbery app is a
new Projection subclass with zero core edits.
"""
