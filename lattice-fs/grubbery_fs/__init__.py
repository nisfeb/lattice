"""grubbery-fs: mount a grubbery app's grub tree as a local filesystem.

The core (this package) knows only "a tree of files/dirs behind a Projection".
All app semantics live in a Projection subclass (see lattice_fs). This is the
future standalone grubbery-fs extraction seam.
"""
